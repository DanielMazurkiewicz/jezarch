// src/styles/index.ts
// This file can be used as the main entry point for CSS generation.
// Import all your .css.ts files here so they are included in the build.

import "./global.css.ts";
import "./theme.css.ts";
import "./utils.css.ts";

// Import styles from components if they define their own .css.ts files
// Example: import "../components/ui/Button.css.ts";

console.log("All styles imported for CSS generation."); // Optional log